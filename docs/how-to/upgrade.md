# How to upgrade

Upgrade the charms with the `refresh` command. The WireGuard gateway charm is designed to automatically migrate any data during the upgrade.

```bash
juju refresh wireguard-gateway
```
